- defaults:
    link:
      relink: true
      force: true

- clean: ['~']

- link:
    ~/.config/qtile: qtile
    ~/.config/dunst: dunst
    ~/.config/rofi: rofi
    ~/.config/kitty: kitty
    ~/.doom.d: doom-emacs

- shell:
  - [git submodule update --init --recursive, Installing submodules]
